# this function detect chest from webcam it will be used in process.py
# it will replace fd.face_detect() of face_detection.py

def chest_detect():
    #this function should take video frames as input and returns:     frame, face_frame, ROI1, ROI2, status, mask 
    # use face_detection.py for reference





    return frame, face_frame, ROI1, ROI2, status, mask